#maxv(3).
#maxbl(4).
#maxhl(1).
#maxpl(6).

% Source: https://github.com/metagol/metagol/blob/master/examples/adjacent-to-red.pl
% Task: Nodes adjacent to a red-colored node.
% Target rules:
% target(A) :- edge(A,B),colour(B,C),red(C).

edge(a,b).
edge(b,a).
edge(c,d).
edge(c,e).
edge(d,e).
colour(a,red).
colour(b,green).
colour(c,red).
colour(d,red).
colour(e,green).
red(red).
green(green).

#pos({target(b)}, {}).
#pos({target(c)}, {}).

#neg({target(a)}, {}).
#neg({target(d)}, {}).
#neg({target(e)}, {}).

#modeh(1,target(var(node,any))).

#modeb(1,edge(var(node,any),var(node,any)),positive).
#modeb(1,colour(var(node,any),var(colour,any)),positive).
#modeb(1,red(var(colour,any)),positive).
#modeb(1,green(var(colour,any)),positive).
#modeb(1,target(var(node,any)),positive).
