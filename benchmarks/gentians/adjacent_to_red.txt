% Source: https://github.com/metagol/metagol/blob/master/examples/adjacent-to-red.pl
% Task: Nodes adjacent to a red-colored node.
% Target rules:
% target(A) :- edge(A,B),colour(B,C),red(C).

edge(a,b).
edge(b,a).
edge(c,d).
edge(c,e).
edge(d,e).
colour(a,red).
colour(b,green).
colour(c,red).
colour(d,red).
colour(e,green).
red(red).
green(green).

#pos({target(b)}, {}).
#pos({target(c)}, {}).

#neg({target(a)}, {}).
#neg({target(d)}, {}).
#neg({target(e)}, {}).

#modeh(1,target,1).

#modeb(1,edge,2,positive).
#modeb(1,colour,2,positive).
#modeb(1,red,1,positive).
#modeb(1,green,1,positive).
#modeb(1,target,1,positive).
