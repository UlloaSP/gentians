#maxv(5).
#maxbl(8).
#maxhl(1).
#maxpl(6).

% Source: Euclid algorithm synthetic benchmark
% Task: Learn gcd recursion used by pairprime/2.
% Target rules:
% eucl(A,B,M) :- num(A),num(B), A < B, eucl(B,A,M).
% eucl(A,Z,A) :- zero(Z), num(A).
% eucl(A,B,M) :- num(A),num(B),zero(Z),A > B,B > Z,D = A \ B,eucl(B,D,M).

#const a=4.
#const b=2.
num(0..A) :- A = #max{a;b}.
zero(0).
pairprime(A,B) :- num(A), num(B), A > 1, B > 1, eucl(A,B,1).
result(M) :- eucl(a,b,M).

#pos({pairprime(4,3), pairprime(3,2), pairprime(2,3), pairprime(3,4)}, {}).

#neg({pairprime(4,2)}, {}).
#neg({pairprime(2,4)}, {}).
#neg({pairprime(1,2)}, {}).
#neg({pairprime(2,1)}, {}).

#modeh(1,eucl(var(numeric,input),var(numeric,input),var(numeric,output))).

#modeb(1,eucl(var(numeric,input),var(numeric,input),var(numeric,output)),positive).
#modeb(1,zero(var(numeric,input)),positive).
#modeb(2,num(var(numeric,input)),positive).
#modecmp(1,lt).
#modecmp(2,gt).
#modearith(1,mod).
