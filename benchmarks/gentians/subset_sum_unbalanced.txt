#maxv(3).
#maxbl(3).
#maxhl(1).
#maxpl(6).

% Source: subset-sum synthetic benchmark
% Task: Unbalanced aggregate variant; generate the selected subset sum.
% Target rules:
% s(S) :- #sum{X : el(X)} = S.

{el(1)}.
{el(2)}.
{el(3)}.
{el(4)}.
{el(5)}.

#pos({s(6)}, {}).

#modeh(1,s(var(numeric,any))).

#modeb(1,el(var(numeric,any)),positive).
#modeagg(1,sum(el/1),unbalanced).
#modeagg(1,count(el/1),unbalanced).
#modecmp(1,neq).
