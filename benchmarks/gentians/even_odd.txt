#maxv(3).
#maxbl(3).
#maxhl(1).
#maxpl(6).

% Source: https://github.com/stassa/louise/blob/master/data/examples/even_odd.pl
% Task: Mutual recursion for even and odd numbers.
% Target rules:
% even(X) :- odd(Y),prev(X,Y).
% odd(X) :- even(Y),prev(X,Y).

even(0).
prev(1,0).
prev(2,1).
prev(3,2).
prev(4,3).

#pos({odd(1), odd(3), even(2)}, {}).

#neg({even(3)}, {}).
#neg({even(1)}, {}).
#neg({odd(2)}, {}).

#modeh(1,even(var(numeric,input))).
#modeh(1,odd(var(numeric,input))).

#modeb(1,even(var(numeric,input))).
#modeb(1,odd(var(numeric,input))).
#modeb(2,prev(var(numeric,input),var(numeric,output))).
