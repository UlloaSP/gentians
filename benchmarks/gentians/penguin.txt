#maxv(3).
#maxbl(3).
#maxhl(1).
#maxpl(6).

% Source: default-negation bird benchmark
% Task: Penguins are birds that do not fly.
% Target rules:
% penguin(X) :- bird(X), not flies(X).

bird(a).
bird(b).
can(a, fly).
can(b, swim).
flies(X) :- can(X, fly).
ability(fly).
ability(swim).

#pos({penguin(b)}, {}).

#neg({penguin(a)}, {}).

#modeh(1,penguin(var(animal,input))).

#modeb(1,bird(var(animal,input)),positive).
#modeb(1,flies(var(animal,input)),negative).
