% Source: default-negation bird benchmark
% Task: Penguins are birds that do not fly.
% Target rules:
% penguin(X) :- bird(X), not flies(X).

bird(a).
bird(b).
can(a, fly).
can(b, swim).
flies(X) :- can(X, fly).
ability(fly).
ability(swim).

#pos({penguin(b)}, {}).

#neg({penguin(a)}, {}).

#modeh(1,penguin,1).

#modeb(1,bird,1,positive).
#modeb(1,flies,1,negative).
