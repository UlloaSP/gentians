#maxv(4).
#maxbl(4).
#maxhl(1).
#maxpl(6).

% Source: triple subset-sum synthetic benchmark
% Task: Return sums that agree across all three tuple positions.
% Target rules:
% ok(S) :- #sum{X,Y,Z : el(X,Y,Z)} = S, #sum{Y,X,Z : el(X,Y,Z)} = S, #sum{Z,X,Y : el(X,Y,Z)} = S.

{el(1,2,3)}.
{el(2,3,5)}.
{el(3,5,1)}.
{el(4,1,3)}.
{el(5,9,4)}.

#pos({ok(0)}, {}).
#pos({ok(9)}, {}).

#neg({ok(1)}, {}).
#neg({ok(2)}, {}).
#neg({ok(10)}, {}).
#neg({ok(15)}, {}).

#modeh(1,ok(var(numeric,output))).

#modeagg(3,sum(el/3),balanced).
