#maxv(6).
#maxbl(4).
#maxhl(1).
#maxpl(6).

% Source: double subset-sum synthetic benchmark
% Task: Return twice a coordinate sum.
% Target rules:
% ok(S) :- #sum{X,Y : el(X,Y)} = A, #sum{Y,X : el(X,Y)} = A, S = A + A.

{el(1,2)}.
{el(2,3)}.
{el(3,5)}.
{el(4,1)}.
{el(5,9)}.

#pos({ok(0)}, {}).
#pos({ok(16)}, {}).
#pos({ok(18)}, {}).

#neg({ok(1)}, {}).
#neg({ok(2)}, {}).
#neg({ok(10)}, {}).

#modeh(1,ok,1).

#modeagg(2,sum(el/2),balanced).
#modearith(1,add).
