#maxv(3).
#maxbl(3).
#maxhl(1).
#maxpl(6).

% Source: subset-sum synthetic benchmark
% Task: Unbalanced aggregate plus extra comparisons variant.
% Target rules:
% s(S) :- #sum{X : el(X)} = S.

{el(1)}.
{el(2)}.
{el(3)}.
{el(4)}.
{el(5)}.

#pos({s(6)}, {}).

#modeh(1,s,1).

#modeb(1,el,1,positive).
#modeagg(1,sum(el/1),unbalanced).
#modeagg(1,count(el/1),unbalanced).
#modecmp(1,neq).
#modecmp(1,geq).
#modecmp(1,leq).
